'use strict'

const c = require('../../src/check-command');

//module.exports = {handleCommand, formatString, handleSend}

describe('check that handleCommand looks for commands', ()=>{
   it('emits it ', ()=>{

   }) 

   it('handleSend', ()=>{

   })

   it('it', ()=>{

   })

});